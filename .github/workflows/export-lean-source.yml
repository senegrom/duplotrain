name: Export Lean source

on:
  push:
    branches: [agent/linear-bound-current]
    paths:
      - .github/workflows/export-lean-source.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: lean-source
          path: theory/lean
          if-no-files-found: error
          retention-days: 1
