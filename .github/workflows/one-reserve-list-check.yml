name: One-reserve list assembly Lean check
on:
  push:
    branches: [agent/linear-bound-current]
    paths:
      - theory/lean/ExternalLobeRoundtrip.lean
      - theory/lean/ExternalLobeReflectionCore.lean
      - theory/lean/FullyLobedComponentTrap.lean
      - theory/lean/OneReserveFromNoTail.lean
      - theory/lean/ReservedComponentCounting.lean
      - theory/lean/OneReserveListAssembly.lean
      - .github/workflows/one-reserve-list-check.yml
  workflow_dispatch:
jobs:
  lean:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: theory/lean
    steps:
      - uses: actions/checkout@v4
      - name: Install Lean
        run: |
          curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf | sh -s -- -y
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Register theorem targets
        run: |
          cat >> lakefile.toml <<'EOF'

          [[lean_lib]]
          name = "ExternalLobeRoundtrip"
          [[lean_lib]]
          name = "ExternalLobeReflectionCore"
          [[lean_lib]]
          name = "FullyLobedComponentTrap"
          [[lean_lib]]
          name = "OneReserveFromNoTail"
          [[lean_lib]]
          name = "ReservedComponentCounting"
          [[lean_lib]]
          name = "OneReserveListAssembly"
          EOF
      - name: Build reserve assembly
        run: |
          export PATH="$HOME/.elan/bin:$PATH"
          lake build OneReserveListAssembly
