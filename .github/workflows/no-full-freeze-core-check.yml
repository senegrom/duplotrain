name: No-full freezing core Lean check
on:
  push:
    branches: [agent/linear-bound-current]
  workflow_dispatch:
jobs:
  lean:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: theory/lean
    steps:
      - uses: actions/checkout@v4
      - name: Install Lean
        run: |
          curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf | sh -s -- -y
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Build hidden-move base
        run: |
          export PATH="$HOME/.elan/bin:$PATH"
          lake build HiddenAbsorb
      - name: Check freezing theorem
        run: |
          export PATH="$HOME/.elan/bin:$PATH"
          lake env lean NoFullFreezeCore.lean
