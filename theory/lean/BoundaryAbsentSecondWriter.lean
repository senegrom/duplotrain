import StateLawCoefficientOneTop
import BoundaryNAddFourSaturation
import ReservedHistoryCharge

/-!
# Compatibility imports for the retained arbitrary-start boundary proof

The shared coordinate-counting lemmas now live in `ReservedHistoryCharge`,
which is independent of the superseded boundary argument.
-/
